
are the following good counters:
* the first math AI to make it to a millennium prize problem will only barely make it to one (is this true? shouldn't we at least expect it to do a lot more later as well?). if there is anything to it that is math-specifically useful, then we should expect it to be worse at non-math, relative to humans. given that it only barely made it to a millennium prize problem and we should expect it to be worse at science (with the level of humans in the two subjects as a reference point), we should expect it to not be able to solve human-similarly hard scientific problems
* (kinda an instance of the previous point:) would it be so weird for us to have used some method that is at least somewhat math-specific?
	* response: yea maybe it's kinda weird. also even if it doesn't work, plausibly something quite close works?

hmm there might be a general connection:
* if a default first PvNP math AI would be scary, then *