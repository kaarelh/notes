
# A sequence theorems saying that P(harm|action) is the only reasonable thing to look at if we want the verdict not to depend on the partition

Below, we give a sequence of theorems, each of which (modulo some details) says that as long as we want the verdict of the guardrail (rejection vs not) to be independent of the chosen partition into $S_1,\ldots,S_k$, the guardrail should just be checking if $P(H|a)<\delta$.

The following theorem says that any reasonable rejection rule whose verdict is independent of the partition just looks at $P(H)$:
* Suppose that 
Proof. 


the following theorem says that any reasonable aggregation rule just looks at $P(H)$:

