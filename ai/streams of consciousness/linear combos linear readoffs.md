
linear readoffs make sense

linear combos are just false? like future model components care about readoffs? and it might not be easiest to create such features? unless we need to compute these features?

freeze first layer at random. then use some features in next layers. for weird features, there's no way we'd recover these. but maybe would recover these by looking at the next layer somehow? 


what equivalence classes does each data point partition the neurons into? 