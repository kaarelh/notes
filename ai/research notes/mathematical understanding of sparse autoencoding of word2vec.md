
two options: running sparse autoencoder on word2vec, or running it on bad of words distribution vectors directly


either way, the simplest case is just vector quantization? might already be "very interpretable"

Q: how similar are the features found in word2vec to those found in a transformer MLP?
Q: what's the reconstruction error if we use those instead for the transformer?

https://aclanthology.org/2021.blackboxnlp-1.12.pdf

