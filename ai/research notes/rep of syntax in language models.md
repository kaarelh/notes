For each token, look at sequence of phrases it is embedded in, look at types, train multioutputput logistic regression probe to predict the type, this will tell us where understanding of whole syntax tree is built in transformer. Maybe the syntax tree is parsed sequantially?


do stanford tree probe paper but also for phrases that make up a sentence, see where understanding of syntax of each is formed

also would be cool to look at above sentence-level syntax, like how are sentences combined into thoughts