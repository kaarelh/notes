
consider a single slit diffraction pattern. there are strongest possible minima, where to first order the amplitude is precisely zero, at places where the phase goes through a full rotation. from a little geometric thinking, this happens where sin theta d = n lambda. but where are the diffraction pattern maxima? and are there also other minima?

we can be boring and solve this algebraically. we are averaging $e^{i\theta}$ from $\theta=0$ to some angle $\alpha$. this is $\frac{1}{\alpha}\int_0^\alpha e^{i\theta}d\theta=\frac{-i}{\alpha}(e^{i\alpha}-1)$. to find local optima of its norm, we compute the derivative of this wrt $\alpha$, ie $\frac{i}{\alpha^2}(e^{i\alpha}-1)+\frac{-i}{\alpha}(ie^{i\alpha}-1)$, and set it to be geometrically orthogonal to the average $\frac{-i}{\alpha}(e^{i\alpha}-1)$, which is equivalent to the real part of $\left(\frac{i}{\alpha^2}(e^{i\alpha}-1)+\frac{-i}{\alpha}(ie^{i\alpha}-1)\right)\overline{\left(\frac{-i}{\alpha}(e^{i\alpha}-1)\right)}$ being $0$. let's bash this out:
$$\left(\frac{i}{\alpha}(e^{i\alpha}-1)-i(ie^{i\alpha}-1)\right)\left(i(e^{-i\alpha}-1)\right)=\frac{-1}{\alpha}(2-e^{i\alpha}-e^{-i\alpha})+i+1-e^{-i\alpha}-ie^{i\alpha}$$
keeping only the real part gives: $$\frac{-1}{\alpha}(2-2\cos\alpha)+1-\cos\alpha+\sin\alpha$$
when i set this to 0 and ask chatgpt to find numerical answers, it's giving some implausible stuff. so i think either i fucked up the algebra or chatgpt fucked up finding numerical solutions to the equation. anyway there should be a solution somewhere between 2kpi and (2k+1/2)pi

oh oops actually this is easy (further edit: nvm). when does an average become larger when we start averaging with some more stuff? in the vector case. what happens is that we slide the old average a tiny bit toward the tip of the new vector thrown in. this increases norm iff the new vector is further from the origin than the old vector, in the direction of the old vector. hmm actually this means that as intensity drops at larger angles, the maxima are shifted slightly later, getting closer and closer to sin theta d = (n+1/2)lambda