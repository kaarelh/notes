
* it is hard to translate [probabilistic claims]/[claims involving probabilities] into a language not involving probabilities
* same for truth
* same for causality
* probably many other examples (it seems worthwhile to try to expand this list?)

what we can do though:
* specify the entirety of a way of thinking. like sit outside everything, say what a bayesian does
* do something more local using "would"/"should"/"could" statements? like relate these to stuff more locally action-facing? like maybe:
	* probabilities -> how one would bet? 
	* truth -> what one should believe?
	* X caused Y -> if X hadn't happened, Y wouldn't have happened?
actually maybe these two are sorta the same? the small is like the big but for a bunch of small cases, said for all of them at once? like sure it's not giving "an algorithm" for you, but it is like giving a bunch of small algorithm-things for a bunch of small situations?