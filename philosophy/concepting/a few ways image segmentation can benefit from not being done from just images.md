
here are some cases in which a human might segment an image correctly when basically nothing just looking at images would:
* you could have text on some object that says "look closely i'm actually two different objects side by side separated at the stripe"
	* you could have text that specifies the separation line without anything being visible there at all
* you could have some structure that can be deduced to be very unlikely to hang together by thinking about physics stuff

ok you could have an "image segmentation" thing trained on just images that like learns language and physics and does sth like the sophisticated thinking a human would do to handle these cases correctly... but it'd be very far from sth doing some simple thing with some low-level data

i mean these examples don't get at this well but generally it seems right that human visual parsing should be quite complicated — like, it uses many ideas, and touches many mental elements


different point on the same topic: i think jan kulveit and the czech EA cluster around him has this "let's make society work" vibe, but