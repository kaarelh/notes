
top latent: 1/3 vs 2/3 bias
and then idiosyncratic randomness in the N singletons for N coinflips with the bias

is there a latent variable model of this where the latents are independent but somehow all the nontrivial ones are at layer <N/2 let's say?
* what would be weird about this?
* well we know there would need to be a huge number of nontrivial latents right. in fact, exponentially many
* 