

setup: We have a system which contains a bunch of theories/advisors/[ways the world could be] — denote these $S_1,\ldots,S_k$. The system is supposed to assign each theory a probability $P(S_i)$, and each theory has an opinion about which actions are good, captured by a function $u_i\colon A\to \mathbb{R}$, where $A$ denotes the set of actions. (In case we only care about what the advisors think the probability of harm is, we take $u_i=P())


have experts S_1, ..., S_n giving their P(H|S_i). have meta-probs about correctness of prob distro. this gives setting to analyze aggregation rules. can we prove one rule rules them all? (might also be nice setup for analyzing some ethics stuff maybe or something)