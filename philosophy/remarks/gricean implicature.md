
or: grice's maxims stuff (mostly added this for searching this doc later)

a sort of argument that you don't have to know if an agent is object-level or meta-level wrt grice's maxims to understand it

(not super carefully thought through)

this stuff might need to be changed when we have multiple agents that know different things about the world, where we don't assume perfect knowledge from the guy talking, or whatever; in general maybe best frame of this is to make the two agents aware of the others' information partition or something, or like what kind of process produced their knowledge. this might let us help make sense of the more general case. also instead of doing the $\varepsilon$ thing might want to do a more reasonable tradeoff between informativity and length (eg seems silly to allow having something much much more informative that is $2\epsilon$ above a proposition)

we consider saying $P$ an appropriate (object-level) conversational move given world $W$ (which, as sort of said above, an agent that knows everything about it is describing in convo) iff among all the propositions of length at most $\varepsilon$ more than the length of $P$, there isn't a single one which is true and obviously implies $P$. (ofc obvious implicature is tricky to make sense of but this seems like a very common issue, and it doesn't even really seem that problematic here. we can just say the proof is very short, at most length $3$, or whatever, or can also have some given reference class of alternatives (which i think is the main option in the lit but silly), or whatever) 

we say P gricean-implies Q if an agent thinking that P is appropriate implies Q — can define this just by quantifying universally over worlds $W$ (?). i think this includes the negation of everything which at most length length $P$ plus $\varepsilon$ and would obviously imply $P$ but is stronger (i.e. true in strictly fewer possible worlds, or equivalently not equivalent i think) — in other words (ignoring change of len when negating), everything different of at most this length which is obviously strictly implied by the negation of $P$ — and then also everything generated by all of these propositions and $P$ i guess (with our current definition); but i think this might be it!

unclear here if maybe want just the generators instead, but whatever for now. or maybe let's call this direct-gricean-implying


now consider the meta-grice rule: suppose we now have an agent that thinks about gricean implicature, and considers it appropriate to say $P$ iff among all the propositions of length at most $\varepsilon$ more than the length of $P$, there isn't a single one which is true and OBVIOUSLY gricean-implies $P$ (this should be equivalent to gricean-implying a superset, which is also a reasonable definition to pick). in other words, there is no true $Q$ of length at most $\varepsilon$ more than $P$ such that an object-level agent thinking that Q is appropriate obviously implies P. in other words, there is no obvious $Q$ of length at most $\varepsilon$ more than $P$ such that the negation of $Q$ obviously strictly implies $P$, or really such that $P$ is in the stuff generated by things obviously strictly implied by the negation of $Q$ of length at most that of $Q$ plus $\varepsilon$ and Q.

it seems plausible that such an obvious Q exists iff there exists a Q with negation obviously strictly implying P, which plausibly is equivalent to the negation of P obviously strictly implying Q, which is equivalent to P directly-gricean-implying-Q

so i think plausibly direct-meta-gricean-implicature is equivalent to direct-gricean-implicature




among similar/simpler sentences, there wasn't anything logically stronger one could have said; this implies what one said was the strongest thing one could have said
in other words, there must not be any simpler (or maybe at most comparably simple) proposition which would have (strictly) implied this one

is this coherent once one thinks of oneself as communicating in a gricean way? like now one can go meta, ie understand one's conversational partner will interpret one's utterance $X$ this way, and come up with the most informative thing $X$ to utter again — i.e., there should never be anything which grice-implies all the stuff $X$ does. is this going to be different? suppose it is: one should say X at first on the object-level, but Y on the grice-conscious level. this means that $Y$ is at most $\epsilon$ more complicated than $X$ but gricean-implies everything which $X$ does and more. ie there is no 





in particular, it must then gricean-imply $X$ itself? 
