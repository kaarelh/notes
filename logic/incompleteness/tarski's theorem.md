
wait i guess it's a triviality. suppose there is a phi('x') which is equivalent to x, provably
* then we could just like in gödel --- see [[diagonalization in logic]] --- construct a sentence which says that it is false
* then we have a contradiction: if it's true, it's false; if it's false, it's true.

could there be a phi('x') which in every model is true iff x is true? again no: because the equivalence itself must then be true in every model, by the completeness theorem, it must be true syntactically, so we have a contradiction

there could be phi('x') which is equivalent to x in particular models though — at least that's not ruled out by this argument. i guess one could maybe just even say "in this model of ZFC, x is true", and this would be semantically equivalent to truth in that model? so i guess that are open wffs equivalent conditional on particular models (tho not very sure i'm not making some mistake here)