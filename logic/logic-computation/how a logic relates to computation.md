
what’s a good way to see each logic (a mode of allowed reasoning; a static thing) as corresponding to a kind of computer (a reasoner; a dynamic thing)?

one concrete relation maybe:
for a system of arithmetic, we can talk about formulas phi such that phi(m)=n always has a proof or disproof for any particular m,n. this is maybe reasonably called the set of functions it can compute? is there an actual nice kind of computer? for peano arithmetic, this is just usual turing-machine-computable functions, right? yea bcs can state TM halting with that on the tape in PA apparently and also anything which can be case by case proved or disproved can be computed by a proofsearching TM (assuming PA is consistent)