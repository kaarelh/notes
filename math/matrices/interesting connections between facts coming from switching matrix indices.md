
* a small (gaussian) data set in high dim has roughly all singular values equal <-> a large (gaussian) data set in small dim has roughly all singular values equal (indeed, the distributions are the same!!!)
* you can pick an exponential-size set of vecs in the dimension which is almost-orthogonal <-> you can approximate the identity matrix with a logarithmic number of random rank-1 matrices (indeed, pick gaussians in each case!)
