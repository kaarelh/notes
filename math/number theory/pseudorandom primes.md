
are all the claims (conjectures) motivated by a picture where primes are pseudorandom just true when instead of asking about numbers which are not 0 mod any p, you just ask for numbers which are not some fixed random sequence of residues mod each p? eg the twin prime conjecture, but instead of looking for twin primes, looking for twins which have some fixed residue mod each prime (and possibly ignoring primes which are larger than the numbers or something) — here does the expectation calculation just go through and then can one do some second moment thing to also get concentration? it would be cool if one could get from this to a for-all result also, but that seems tough