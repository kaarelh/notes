
![[Pasted image 20241206031405.png]]


pf. multiply r into LHS, then expand as rH(X_1)+rH(X_2|X_1)+...+rH(X_d|prev) r times. now also expand RHS in the same ordering. now group the X_i terms on the RHS, of which there are at least r. note that each one is conditioning on fewer things than the X_i thing on the LHS, and so larger than that. so RHS is larger than LHS!