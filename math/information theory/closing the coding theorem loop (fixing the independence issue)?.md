

so we have some coding scheme where the codewords are near-independent in the sense of entropy. can we come up with one (with the same rates) where the codewords are actually independent given the top codeword?

would like to just use the same distinctions. but that creates dependence! then would like to put some bits from the distinctions to the top and now have conditional independence. but this isn't possible for general random variables — you can have very small mutual information while having very large common information. could it be possible for the distinctions here? 