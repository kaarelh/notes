
The point is to show that all singular values are at the same scale for a sufficiently rectangular matrix.
* We can show this by showing that the sum of squared projections to any axis is at some particular scale (for all axes at once). (Since the square of each singular value is this quantity for some particular axis, this implies that the singular values are at the same scale.)
* We do this by showing it for a net with a union bound, and arguing that for a sufficiently fine net, the property holding for the net implies that it holds for any vector.
* A little calculation shows that it's fine to take a net on the unit sphere $S^{n-1}$ with distance $\sim 1/n$, and another little calculation shows that there is such a net of size $e^{c n\log n}$ for some const $c$.
* So, it suffices to show the claim for 

Ahh actually! The dual argument is much nicer! ie argue about directions for the transpose instead. Then we can argue that at any point in the process, there is no direction in the span with very low variance. This is just considering any possible such direction as a linear combo of a direction in the previous span and the direction orthogonal to the previous span, and noting that as long as previously the min was $(1-\varepsilon)$ and there are only a small frac of the dim vectors (probably like $1/\log^k d$ for some small const $k$ will do, so not that small a fraction), then 'each component will give its appropriate amount' to the sum of squared projections to that direction, so we still get $(1-\varepsilon)$. The only important thing for this argument to work is that on every step, the next vec needs to be pretty orthogonal to the span of the previous ones. In some sense this is also best possible, because once the number of vecs gets to be a large enough fraction of the dimension for this simple orthogonality condition to probably fail for some vector, then once it fails, you can't make up for the lost mass in the orthogonal complement of the first vecs before that ever! hmm wait why does this imply one sing value small though, as opposed to this being spread out? we plausibly get that the sum of some sing vals is small, but why does some sing val then need to be big or small? the smallness could be spread out? it seems plausible now that a shit direction would be saved later on i guess? i mean actually no. the contributions from later things to that shit direction are like 1/d each right, so you only get saved by $o(1)$ with high probability in total? need this to be true for all at once, but plausibly happens i guess? need this one sum to be concentrated after the direction gets fucked. this probably happens right! so we are fine. indeed this argument is best possible i think. after you get to more than whatever this argument gives many directions, you will get some direction with less than $(1-\varepsilon)$ mass. I guess this is even best possible for any $\varepsilon$? Like does it work even for less than $0.01$ mass in a direction? ahh yes i guess the original argument does give you this yes, even at like $99/100$ of everything you have concentration around $1/100$ for the last ones right, so this is fine. the point is that the dimension remaining is still macroscopic.

wait ok sure this gives that there isn't a direction with very low variance, maybe even with the right dependence. but why isn't there a direction with very large variance? don't we need a union bound here anyway? 

fact: the actual sing vals are the majorizing square sum sequence of sing vals (this is just because svd indeed gives a sequence of highest-proj vals)

## a determinant and trace argument

We want to show all values are the same-ish. Note that it roughly suffices to show that the determinant is as big as possible for the given trace, because the AM>=GM equality case has all the numbers being equal. (Well, of course, we need to be track the distance quantitatively here — a priori idk if it'll work.) 

So, we take an $m\times d$ gaussian matrix $F$ with $m$ somewhat larger than $d$; we wish to understand the eigenvalues of the $d\times d$ matrix $\frac{1}{m}F^T F$. The idea is to do so by understanding its trace and its determinant. Its trace is the sum of its diagonal entries; if we let $f_i$ denote row $i$ of $F$, then this trace is $\frac{1}{m}\sum_{i=1}^m \lVert f_i\rVert^2$, which has expected value $\frac{1}{m}m=1$ and standard deviation $\frac{1}{m}\sqrt{md}\frac{1}{d}=\frac{1}{\sqrt{md}}$. This immediately gives us (using Chebyshev) that with probability $1-o(1)$, the trace is in the range $1\pm \frac{g}{\sqrt{md}}$, where $g$ is any function of $md$ which goes to $\infty$ (e.g., $g=\log \log \log (md)$). Its determinant is a sum of $d!$ products of terms like $\frac{1}{m}\sum_{i,j}f_i\cdot f_j$. We want to say this huge thing is concentrated around a certain value, I think. One option here is to compute its expectation and variance. The expectation of a product of te

This requires evaluating $(d!)^2$ covariances. One of these covariances looks like 