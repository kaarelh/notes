
The general form of a local isometry on $U$ modulo an internal isometry (which is irrelevant wrt changing masses on coordinates) is $$U\mapsto U+U^\perp C^T$$ for a $d\times (m-d)$ infinitesimal matrix $C$. The first-order mass change this causes in coordinate $i$ is $$(2UC(U^\perp)^T)_{ii}.$$


