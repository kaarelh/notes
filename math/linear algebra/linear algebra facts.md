
If a quadratic form is $\geq 0$ on a basis, then it is is PSD in general. This should follow from the C-S inequality.

Also, any PSD matrix factors as a product of two matrices — just take the diagonalization, take square roots on the diagonal (because all non-negative can do that). Anyway, this lets one go from zero quadratic form value on a basis to the condition that the quadratic form must be 0?

Can argue from here that any symmetric configuration lacks a principaler component?